<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ReciclaApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:fontFamily">@font/plex_sans_condensed</item>
    </style>

    // used for Date Picker in OrdenHorarioActivity
    <style name="CustomDatePickerDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/green</item>
        <item name="colorControlActivated">@color/green</item>
        <item name="android:selectableItemBackgroundBorderless">@color/green</item>
        <item name="colorControlHighlight">@color/green</item>
    </style>

    <style name="Theme.ReciclaApp" parent="Base.Theme.ReciclaApp" />
</resources>